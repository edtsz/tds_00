#include <stdio.h>

int main() {
    int numeros;
    int impares = 0;
    int resto;

    printf("Digite 10 numeros:\n");
    for (int i = 0; i < 10; ++i) {
        printf("Numero %i: ", i + 1);
        scanf("%i", &numeros);
            resto = numeros%2;
            if (resto>0){
                impares++;
            }
    } 
      printf("Total de numeros impares: %i\n", impares);

    return 0;
}
