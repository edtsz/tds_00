#include <stdio.h>

int main() {
    int numeros[10];
    int impares = 0;
    int resto;-

    printf("Digite 10 numeros:\n");
    for (int i = 0; i < 10; ++i) {
        printf("Numero %i: ", i + 1);
        scanf("%i", &numeros[i]);
    } 
    for (int i = 0; i < 10; ++i) {
        if (numeros[i] % 2 != 0) {
            printf("%i é impar.\n", numeros[i]);
            impares++;
        }
    }printf("Total de numeros impares: %i\n", impares);

    return 0;
}
